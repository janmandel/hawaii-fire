name: hi-fire
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10  # Specify the Python version you need
  - numpy=2.1.2
  - pandas=2.2.3
  - matplotlib=3.9.2
  - netCDF4=1.6.4
  - rasterio=1.4.1
  - geopandas=1.0.1
  - dbfread=2.0.7
  - pygrib=2.1.6
  - joblib=1.4.2
  - psutil=6.0.0
  - pip=24.2  # Ensure pip is available for any pip-only packages
  # Optional: Add any additional dependencies below
  # For example, if you need specific versions or additional packages
